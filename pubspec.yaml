name: kayseri_gezi_rehberi
description: A new Flutter project.
publish_to: 'none' # Remove this line if you want to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^0.13.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/places.json
    - assets/erciyes.PNG
    - assets/cm.PNG
    - assets/kbş.jpeg
    - assets/ys.PNG
    - assets/ktp.PNG
    - assets/ss.PNG
    - assets/